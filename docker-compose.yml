version: '3.8'

services:
  tripcraft:
    build:
      context: .
      dockerfile: Dockerfile
    image: tripcraft:latest
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
